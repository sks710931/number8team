import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette:{
        primary:{
            main: '#fee900',
            dark: '#fee900',
            light:'#fee900',
        },  
    },
    typography:{
        fontFamily: "'Vcr', 'Excel'"
    }
});
